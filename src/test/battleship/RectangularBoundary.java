package test.battleship;

import main.battleship.objects.Coordinate;
import main.battleship.objects.IBoundary;

public class RectangularBoundary {
    void testBoundaryContains(){
        IBoundary rectBoundary = new main.battleship.objects.RectangularBoundary(new Coordinate(0,0,))
    }
}
